<?xml version="1.0" encoding="UTF-8"?>
<!-- Safe baseline: console at INFO. This config never fails to load.
     Remote (Logstash) logging is applied at runtime by at.rtr.rmbt.config.LoggingConfigurer,
     which reads the deployment context (Tomcat conf/context.xml <Parameter> entries, or JVM
     system properties / environment variables):
       - LOGGING_CONFIG_FILE set to an existing file -> that logback config is loaded verbatim
       - LOG_HOST set                                -> Logstash (INFO) + console (ERROR only)
       - neither                                     -> this console config stays in effect -->
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
